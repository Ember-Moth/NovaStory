[test]
preload = ["./src/test/preload-db.ts"]

[install]
# Keep a single React instance for hooks (rpc, jotai, wouter, etc.).
dedupe = ["react", "react-dom"]

[serve.static]
plugins = ["bun-plugin-tailwind"]
env = "BUN_PUBLIC_*"